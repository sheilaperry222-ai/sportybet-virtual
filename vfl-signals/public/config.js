/*
 * config.js — where should the browser connect for live predictions?
 *
 *   ""                    → same server that serves this page (local dev, or if
 *                           you host everything on one Node host like Render)
 *   "https://your-backend.example.com"  → separate Socket.IO backend (the
 *                           standard setup when the frontend is on Netlify)
 */
window.VFL_SOCKET_URL = "";
